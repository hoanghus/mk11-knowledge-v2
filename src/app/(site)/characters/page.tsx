import { PageTemplate } from "@/components/shared/page-template"

export default function CharactersPage() {
  return <PageTemplate title="Characters" description="Character index with filters by difficulty and archetype." />
}
