import { PageTemplate } from "@/components/shared/page-template"

export default function FrameDataPage() {
  return <PageTemplate title="Frame Data" description="Data-heavy page for search, filter, and sort by move properties." />
}
